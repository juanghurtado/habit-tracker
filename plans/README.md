# Implementation Plans

Generated by the improve skill on 2026-06-25 at commit `bcfcb39` (batch 1) and `581ee39` (batch 2). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add vitest, jsdom, and write initial unit tests | P1 | M | — | DONE |
| 002 | Add React error boundary to prevent full-app crashes | P1 | S | — | DONE |
| 003 | Fix EditHabitSheet stale-state flash with key-based remount | P1 | S | — | DONE |
| 004 | Add crypto.randomUUID fallback for older browsers | P1 | S | — | DONE |
| 005 | Fix undoLastCompletion to use timestamps, not array order | P2 | S | 001 | DONE |
| 006 | Fix UTC/local date boundary mismatch in completion matching | P2 | M | 001 | DONE |
| 007 | Remove unused exports (HabitCard, getButtonColor, findPaletteColor, formatUTCDateKey) | P2 | S | — | TODO |
| 008 | Memoize stats computation in StatsPage | P2 | S | — | TODO |
| 009 | Set up Ultracite for linting and formatting | P2 | M | 007 | TODO |
| 010 | Add unit tests for useHabits hook and sheet components | P1 | M | 007 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 005 and 006 require 001 because they change storage/query logic that needs characterization tests first.
- 009 recommends 007 first — dead code triggers lint warnings that muddy the initial `ultracite check` output.
- 010 recommends 007 first — ensures the deleted `habit-card.tsx` isn't mistakenly tested.

## Findings considered and rejected (batch 1)

- **Icon lookup prototype chain**: Only exploitable via manual localStorage tampering; the `?? Trophy` fallback covers all realistic icon names. Not worth the churn.
- **Missing aria-labels on all controls**: Real access issue, but MED effort across many files. Deferred — would benefit from a systematic pass with an a11y audit tool, not piecemeal.
- **Add/edit habit sheet duplication (refactor)**: Valid but conflicts with the simpler `key`-based fix in plan 003. Revisit if form fields grow beyond the current set.
- **`@/*` path alias unused**: Trivial config drift. Not worth a plan — address when the next import is added.
- **Stale git worktree**: Operational, not code. Prune with `git worktree remove`.
- **`notifyListeners()` re-reads localStorage**: Minor efficiency concern at current data volumes. Revisit if the app scales.
- **`getCompletionsForHabitOnDate` un-memoized**: O(habits × completions) but the app is small. Plan 006 touches this function — address memoization there if at all.
- **`npm run deploy` branch guard**: Valid but one-line fix; too trivial for a plan.
- **No linter/formatter**: Addressed by plan 009 (Ultracite).

## Findings considered and rejected (batch 2)

- **`npm audit`**: 0 vulnerabilities. No finding.
- **`createCompletion` noon-UTC vs real-time inconsistency**: Deliberate design from plan 006; the `getCompletionsForDate` local-range filter makes it correct. No bug.
- **`useHabits` module-level cache pattern**: Correct for single-user app; over-engineering complaint isn't worth a plan.
- **`getIcon` prototype-chain fallback**: Already rejected in batch 1 as localStorage-tampering only.
- **No dark mode toggle**: ADR 0002 specifies system-preference respect, implemented correctly in `index.css`. Not a gap.
- **Stats re-computation**: Addressed by plan 008 (useMemo).
- **Dead code `getButtonColor`/`findPaletteColor`/`formatUTCDateKey`/`HabitCard`**: Addressed by plan 007.
- **No UI-layer tests**: Addressed by plan 010.